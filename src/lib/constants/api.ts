// ─── External API endpoints ───────────────────────────────────────

/**
 * Free exchange rate API (open.er-api.com).
 * Returns latest rates with the given base currency.
 * Does NOT support historical dates on the free tier.
 */
export const EXCHANGE_RATE_API_URL = `https://open.er-api.com/v6/latest`;
